POSTGRES_HOST=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Admin account created by the seed in development/test. Change before seeding.
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=changeme

# Without an explicit render: HomeController#index → app/frontend/pages/home/index.vue
INERTIA_DEFAULT_RENDER=true
